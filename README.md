# Sadbhav Portfolio (React + Vite)

This is a React + Vite based portfolio for placements and interviews.

## Run locally

```bash
npm install
npm run dev
```

Then open the URL shown in the terminal (usually http://localhost:5173).